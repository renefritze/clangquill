{# Preprocessor macros have no SymbolKind in the IR yet; this template is a
   ready customization seam. C macros live in the C domain (`c:macro`). #}
{% set comment = gen.comment(symbol) %}
```{c:macro} {{ symbol.signature or symbol.spelling }}

{{ render_comment(comment) }}
```
